-- Complete Quantum Computing Test
-- This demonstrates circuit creation, gate application, and simulation

let complete_quantum_test = (
    -- Step 1: Create a 2-qubit Bell state circuit
    let circuit = quantum_circuit(2) in
    let _ = hadamard(circuit, 0) in
    let _ = cnot(circuit, 0, 1) in

    -- Step 2: Simulate the circuit
    let result = simulate_circuit(circuit) in

    -- Return both circuit and result
    result
)

-- Execute the complete test
let final_result = complete_quantum_test
