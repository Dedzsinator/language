-- Matrix Language Working Test Suite
-- Tests all basic functionality that should be working

-- =============================================================================
-- SECTION 1: CONSTANTS AND BASIC MATH
-- =============================================================================
let header1 = println("=== CONSTANTS AND BASIC MATH ===")

-- Test constants
let pi_test = println(pi)
let e_test = println(e)
let tau_test = println(tau)

-- Test basic math functions
let abs_test = println(abs(-5))
let sqrt_test = println(sqrt(16.0))
let sin_test = println(sin(pi))
let cos_test = println(cos(0.0))

-- Test stdlib math functions
let tan_test = println(tan(0.0))
let exp_test = println(exp(1.0))
let log_test = println(log(2.718281828))
let pow_test = println(pow(2.0, 3.0))
let floor_test = println(floor(3.7))
let ceil_test = println(ceil(3.2))
let round_test = println(round(3.6))
let max_test = println(max(10, 20))
let min_test = println(min(5, 3))

-- =============================================================================
-- SECTION 2: STRING FUNCTIONS
-- =============================================================================
let header2 = println("=== STRING FUNCTIONS ===")

-- Test str function directly
let str_int = str(42)
let str_float = str(3.14)
let str_bool = str(true)
let str_results = println("String conversion test complete")

-- =============================================================================
-- SECTION 3: PHYSICS FUNCTIONS
-- =============================================================================
let header3 = println("=== PHYSICS FUNCTIONS ===")

-- Test physics world creation
let world = create_physics_world()
let world_msg = println("Created physics world")

-- Test object creation
let object1 = add_rigid_body(world, "cube", 1.0, [0.0, 0.0, 0.0])
let object_msg = println("Added rigid body")

-- Test physics step
let step_result = physics_step(world)
let step_msg = println("Physics step completed")

-- Test object queries
let position = get_object_position(world, object1)
let mass = get_object_mass(world, object1)
let shape = get_object_shape(world, object1)
let objects = list_objects(world)
let query_msg = println("Object queries completed")

-- =============================================================================
-- SECTION 4: QUANTUM FUNCTIONS (BASIC)
-- =============================================================================
let header4 = println("=== QUANTUM FUNCTIONS ===")

-- Test quantum circuit creation
let circuit = quantum_circuit(2)
let circuit_msg = println("Created quantum circuit")

-- Test basic gates
let h_gate = h(circuit, 0)
let x_gate = x(circuit, 1)
let cnot_gate = cnot(circuit, 0, 1)
let gates_msg = println("Applied quantum gates")

-- Test simulation
let sim_result = simulate_circuit(circuit)
let sim_msg = println("Simulated quantum circuit")

-- =============================================================================
-- FINAL MESSAGE
-- =============================================================================
let final_msg = println("=== ALL TESTS COMPLETED ===")
