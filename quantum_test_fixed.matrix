-- Simple Quantum Test for Matrix Language
-- Tests basic quantum functionality

let test_quantum = () => {
    print("Testing quantum computing functionality...");

    -- Create a simple 2-qubit circuit
    let circuit = quantum_circuit(2);

    -- Apply basic gates
    hadamard(circuit, 0);
    cnot(circuit, 0, 1);

    -- Simulate the circuit
    let result = simulate_circuit(circuit);

    print("Circuit created and simulated successfully!");
    print("Bell state entanglement test completed.");

    result
};

-- Execute the test
test_quantum()
