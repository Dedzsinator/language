-- Test script for 3D object creation integration
-- This script tests the fixed parser with correct comment syntax

-- Basic variables
let x = 5
let y = 10.5

-- Function definition
let add = (a: Int, b: Int) -> Int => a + b

-- Create a cube object
let cube = {
    name: "TestCube",
    position: Vec3::new(0.0, 0.0, 0.0),
    rotation: Vec3::new(0.0, 0.0, 0.0),
    scale: Vec3::new(1.0, 1.0, 1.0),
    color: [1.0, 0.0, 0.0, 1.0]
}

-- Create a sphere object
let sphere = {
    name: "TestSphere",
    position: Vec3::new(2.0, 0.0, 0.0),
    rotation: Vec3::new(0.0, 0.0, 0.0),
    scale: Vec3::new(1.0, 1.0, 1.0),
    color: [0.0, 1.0, 0.0, 1.0]
}

-- Calculate result
let result = add(x, 3)
