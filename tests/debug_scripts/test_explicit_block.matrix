-- Test with explicit block and return statement
let arr = [1, 2, 3, 4, 5]
let max_length = 3

let test = () => {
    if arr.length() > max_length {
        let _ = arr.remove(0)
        42
    } else {
        0
    }
}
